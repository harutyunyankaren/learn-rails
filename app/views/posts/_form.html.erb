<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.collection_select(:category_id, Category.all, :id, :name, {prompt: 'Choose a category' }) %><br>
  <%= f.label :title %><br>
  <%= f.text_field :title, placeholder: 'Type the post title here' %><br>
  <%= f.label :content %><br>
  <%= f.text_area :content, size: '40x6', placeholder: 'Type the post tect here' %><br>

  <%= f.submit %>
<% end %>
